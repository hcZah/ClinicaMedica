import { EspecialidadeMedico } from './especialidade-medico';

describe('EspecialidadeMedico', () => {
  it('should create an instance', () => {
    expect(new EspecialidadeMedico()).toBeTruthy();
  });
});
